# Mission Control 1.9.0

Upload `Richard-Brummer-SEO-Mission-Control-v1.9.0.zip` in Plugins → Add Plugin → Upload Plugin.

After activate: LiteSpeed Guest Mode OFF, purge cache, open Richard Brummer SEO → Run full audit now.

Google key (optional):

```php
define( 'RBSMC_GSC_PROPERTY', 'sc-domain:luxetrendsetters.com' );
define( 'RBSMC_GSC_KEY_FILE', '/secure/non-public/service-account.json' );
define( 'RBSMC_AMAZON_TAG', 'yourtag-20' );
```
